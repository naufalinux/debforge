# Exported: repo-root `.gitignore`

> Source file: `.gitignore` (repo root). Not natively Markdown — wrapped in a fenced block below for this export.

```gitignore
# Local env files (never commit real secrets)
.env
*.env.local

# Logs generated by the setup scripts
setup-dev.log
*.log
```
